/* Aggregator header for translate-c: the minimal FreeType surface the backend needs.
 * FreeType requires ft2build.h first; it defines the macros the other headers rely on. */
#include <ft2build.h>
#include <freetype/freetype.h>
#include <freetype/ftoutln.h>
